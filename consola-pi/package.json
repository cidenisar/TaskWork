{
  "name": "consola-pi",
  "version": "0.1.0",
  "description": "Consola Disparadora — software real que corre en la Raspberry Pi de cada sitio. MQTT + validación de PIN local + relé/sirena + botonera física. Ver README para el estado (todavía sin pantalla táctil).",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/pantalla && cp src/pantalla/index.html dist/pantalla/index.html",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^13.0.3",
    "dotenv": "^16.4.5",
    "mqtt": "^5.10.0",
    "serialport": "^13.0.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
